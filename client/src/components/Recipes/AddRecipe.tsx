import React from "react";

interface AddRecipeProps {}

const AddRecipe: React.FC<AddRecipeProps> = ({}) => {
  return <section className="section">Add a recipe</section>;
};

export default AddRecipe;
